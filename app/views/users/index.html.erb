<!-- ADMIN VIEW ALL USERS -->
<h3>All Users</h3>
<section class="col-md-12">
<% @users.each do |user| %>
	
	<h4><%= user.username %></h4>
	<div class="user-avatar">
		<%= image_tag(user.avatar) %>
	</div>
	<%== admin_nav %>

<% end %>
</section>
